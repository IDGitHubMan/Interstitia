function setup() {
    size(100, 100);
    let thumbs = selectAll('collection');
    for (var thumb of thumbs) {
        console.log(thumb.style("background-image"))
    }
}

function draw() {
    let thumbs = document.getElementsByClassName();
    print("UGH!")
    for (var thumb of thumbs) {
        print(thumb.style("background-image"))
    }
}